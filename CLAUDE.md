# ccswitch

A minimal, macOS-only command to switch between several Claude Code OAuth accounts. Single bash script; the Keychain (`Claude Code-credentials`) is the source of truth.

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in `saiday/claude-code-multi-login` (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
